/// <reference types="vite/client" />

// Injected at build/dev time by vite.config.ts `define`, sourced from
// package.json "version" — the single source of truth for the app version.
declare const __APP_VERSION__: string
